const constants = {};

export default Object.defineProperties(constants, {
    SEND_SDS_SERVICE         : { value: 'send_sds_service', writable: false },
    SEND_PATTERNS_ACTIVE         : { value: 'send_patterns_active', writable: false },
})
