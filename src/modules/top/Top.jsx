import React from 'react';
import {
    Wrapper
} from './StyledComponent.js';

const Top = () => {
    return (
        <Wrapper>
            Top
        </Wrapper>
    );
}

export default React.memo(Top);